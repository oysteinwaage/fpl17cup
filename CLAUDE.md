# Claude Code instructions

- Always ask the user first before using Claude Preview (`preview_start`, `preview_eval`, `preview_click`, etc.) or spinning up a dev server (`vercel dev`, `npm start`, ...) to test a change. Never do this automatically just because a change is observable in the browser — confirm with the user each time before starting/using the preview tools.
